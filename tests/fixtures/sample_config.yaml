cluster:
  name: test-cluster
  namespace: raibid-ci

api:
  host: 0.0.0.0
  port: 8080
  log_level: info

agents:
  rust:
    enabled: true
    max_replicas: 3

redis:
  host: localhost
  port: 6379

gitea:
  url: http://localhost:3000
  admin_user: admin
